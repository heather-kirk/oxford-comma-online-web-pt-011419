def oxford_comma(array)
  when l
    ""
  when 2 
  array.join
  when 3 
    
  if array.length == 1
array.join
elsif array.length == 2
array.join(" and ")
else array.length >= 3
  array[0..2].join(", ") + ", and" array[-1]
  end
end
